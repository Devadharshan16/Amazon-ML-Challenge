# Core data processing
pandas>=2.0.0
numpy>=1.26.0

# String similarity (fast C-backed Levenshtein, Jaro-Winkler, etc.)
rapidfuzz>=3.6.0

# Phonetic algorithms (Soundex, Metaphone)
metaphone>=0.6

# ML model
lightgbm>=4.3.0
xgboost>=2.0.0
scikit-learn>=1.4.0

# TF-IDF + approximate nearest neighbors for blocking
sparse_dot_topn>=1.1.0       # fast sparse matrix top-N for TF-IDF ANN

# Progress bars
tqdm>=4.66.0

# Serialization / caching
joblib>=1.3.0

# Parquet support (required for pandas .to_parquet / .read_parquet)
pyarrow>=14.0.0
